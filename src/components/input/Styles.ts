import {StyleSheet} from "react-native";

export default StyleSheet.create({
    input: {
        borderColor: "#E3E5E6",
        borderWidth: 1,
        paddingHorizontal: 8,
        borderRadius: 8,
        height: 40
    },
    touchableStyles: {
        marginTop: 32,
        borderRadius: 8
    }
})
